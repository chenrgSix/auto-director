# 迭代记录

## 2026-09-12 · C39 把可确定的工作流错误提前拦截

- 在素材生成和上传前校验实际生效图与全部输入素材，错误指出工作流及节点字段，合法用户覆盖不被失效默认阻塞；既有素材复用和 UNKNOWN 快照恢复保持。
- AI 与参数页面获得完整源/下游约束及步长起点，配置问题可直接定位到可编辑源参数；导入仍无需等待 ComfyUI，实时检查补齐元数据。
- 完整门禁 **549 passed**，新增 27 项回归，隔离浏览器及最终前端检查通过。正式短片仍在生成，本轮保持原后端进程，新增预检将在下次重启加载。

## 2026-09-12 · C38 修复经整数节点输入的非法视频尺寸

- PrimitiveInt/PrimitiveFloat 直连下游节点时继承实际消费者约束，自动尺寸统一适配，手填非法值在生成前报错；不猜测数学节点的计算结果。
- 旧工作流和失败片实时刷新即可生效，无需重新导入、重写剧本或删除已有素材。
- 完整 make check **522 passed**（150.55 秒），新增 18 项回归；真实 MiniMax 作业无提交复核确认 384×688 调整为 384×672，最终图仅高度节点变化。本地服务已更新，97 条记录、36 个资产及配置保持。完整验收见 C38 记录。

## 2026-09-12 · C37 已有剧本可切换生成质量

- 详情页增加质量模式编辑，保存后保留剧本、提示词、已绑定图片/视频和画面尺寸，由用户决定何时继续生成。
- 候选数、质检门槛和模式默认重试次数同步生效，取消后的旧重试游标不会把新模式卡在耗尽状态；旧配置、作业和资产保留。
- 增加版本冲突、活动/未结算任务保护，保存不调用模型或渲染服务。验收结果见 C37 记录。
- 完整 make check **504 passed**（154.94 秒），17 项新增回归；最终前端检查和隔离浏览器验证通过。正式服务已更新，89 条记录、34 个资产与在线配置散列保持；未代用户切换模式或提交真实生成。

## 2026-09-12 · C36 镜头数量与时长以剧情为主

- 移除“优先最少镜头、每镜接近上限”的导演引导，镜头数量和时长按剧情决定；2.5 秒改为软提示，合理短镜与有意等长均允许，用户明确固定时长继续优先。
- 自动长片分批给剧情拆镜留空间，为后续批次保留必要镜头额度；总长、每镜 1 秒/工作流上限、最多 240 镜和百分之一秒归一化保持。
- 补齐 30/60/90 秒不同节奏、600 秒额度预留、预览到最终视频时长的回归。旧分镜、素材和提示词重跑语义保持；验证结果见 C36 验收记录。
- 完整 make check **487 passed**（143.72 秒），新增 14 项回归，正式 8000 已更新；54 条记录、21 个资产散列与在线配置保持。未提交真实模型或渲染任务。

## 2026-09-12 · C35 重跑支持重新生成提示词

- 新增「重新生成提示词、关键帧及视频」，适用于单镜和整片；调用 Shot Agent 重写所选镜头的完整提示词及 AI 参数，保留分镜故事/时长和视觉设定。
- 明确选中的连续镜头也会重写，间接依赖只重做必要素材；定格和高级固定覆盖继续优先。失败续跑保留已完成的新提示词。
- 旧提示词与旧视频一起归档，可在重跑历史查看。原两种模式、未知提交保护与旧数据兼容。
- 完整 make check **473 passed**（142.36 秒），13 项新增回归，隔离浏览器验收通过。正式服务已更新，最新 54 条记录与 21 个资产散列保持，未重跑真实任务。

## 2026-09-12 · C34 工作流 AI 识别使用模型超时配置

- 去掉识别接口独立的 45 秒总上限，改用当前在线 llm_timeout；设置 600 秒或自定义值后，下一次识别立即沿用。
- 前端说明指向模型配置，不新增页面固定等待上限；取消识别/关闭页面仍取消后台请求，超时提示包含实际秒数。
- 新增在线改值及 Provider 共用超时回归，更新原超时取消与断线用例。完整 make check **460 passed**，正式服务已加载，203 条记录与 104 个资产散列跨重启保持。

## 2026-09-12 · C33 修正近乎相同的首尾帧

- 当前镜头提示词优先，去除与尾帧终态冲突的全片人数/场景和上一镜状态；参考图保持身份，不强制重复姿态与构图。
- 首尾帧视频新增独立于 VLM 的本地近重复门禁，预算内仅重试尾帧，耗尽暂停视频；保留旧资产、用户覆盖和原任务恢复。
- 分镜预览与重跑可声明有意定格；旧数据和接口保持兼容。
- 已修正「MiniMaxH3-文生图（参考图）」RandomNoise.noise_seed 的 system/seed 绑定，未改原 JSON 种子或参考保真参数，未重跑真实影片。最终门禁与数据保持证据见 C33 验收。

## 2026-09-12 · C32 修正新导入 MiniMax 的帧数时钟

- 将「minimax-h3-首尾帧-质量」的 duration 绑定修正为 17n+5、24 FPS，保留 6 秒上限，解除通用 4n+1/16 FPS 与模型步长冲突造成的 4.5 秒限制。
- 实时 13 类节点预检覆盖全部 6×5 秒，最终 length=124/fps=24；相关 16 项测试通过。当前短片恢复可继续状态，全部故事/提示词、9 张图片和历史作业保持，只有指定 Workflow/Episode 改变。
- 空闲时重启正式服务并加载 C31，66 条记录跨重启保持，被删模板没有恢复。未自动生成真实视频。

## 2026-09-12 · C31 删除工作流后保持删除结果

- 内置模板仅在首次初始化配置时添加，后续重启尊重删除；兼容旧默认能力配置、空工作流列表和初始化中断。
- 删除被默认/短片引用拦截时明确显示“未删除”和占用短片名称，避免将拒绝误解为删除后恢复。新增自导入与内置工作流跨重启回归。
- 完整 `make check` **445 passed**（142.87 秒），8 项新增回归，前后端检查及构建通过。
- 已移除用户指定的内置首尾帧模板，其他记录保持；MiniMax 两份当时仍有默认/短片引用，未擅自删除。用户视频运行期间未重启；现已随 C32 在空闲时加载。

## 2026-09-11 · C30 恢复 OOM 降级中的原作业

- 持久化镜头尝试与恢复步骤，断网/重启后沿用原 prompt_id、配置快照和重试余量，覆盖降分辨率、分段及中间帧。完成过的步骤与 OOM 决策复用，未知提交继续保护，旧数据兼容。
- 新增 12 项恢复回归，C27–C30 完整 **437 项测试通过**，前后端检查与构建通过。正式 8000 已加载；130 条原记录未变，当前短片 6 镜剧本及 9 张图片保持，未提交真实渲染。远端 OpenVDN 目录缺失仍需修正环境。

## 2026-09-11 · C29 分镜不再因连接检测失效

- 区分工作流执行配置版本与普通检查记录，重复检测、名称更新和无变化保存保持已准备预览有效。
- 参数、绑定、能力变更仍要求更新预览，实际执行仍检查实时约束。9 项新增及 65 项相关测试通过。

## 2026-09-11 · C28 拦截过短镜头视频

- 新视频、缓存和重新导出均校验实际视频流时长，过短输出在当前镜头失败并按预算仅重试视频，避免浪费后镜渲染。
- 继续生成可修复旧版错误通过的短视频并更新连续依赖，保留历史产物、故事和参考图。7 项新增及 41 项既有相关测试通过。

## 2026-09-11 · C27 帧数上限参与导演规划

- 修复 length/num_frames 绑定跳过时长约束的问题，统一规划、预览、Resolver 与最终 patch 的 FPS、倍数、偏移和节点限制。
- 保存规划时的视频参数快照，读取预览不丢失实时约束；无合法帧数在生成图片前拒绝，旧故事保持。相关 **76 项测试通过**。

## 2026-09-11 · C26 清理失效警告与恢复当前短片

- 更换为带 reference_image 绑定的图像工作流后清理旧警告；读取旧短片兼容更新显示，继续生成时持久化同步，保留其他诊断与旧工作流历史。
- 完整 **399 项测试通过**。正式服务已加载 C24–C26，当前失败短片预算修正为 384×672，保留 6 镜故事、7 张参考图和已有首尾帧；页面可继续生成，未触发真实渲染。

## 2026-09-11 · C25 自动尺寸与工作流对齐

- 自动分辨率按实际工作流范围、步长和枚举向下取值，避免 688 高度违反 MiniMax 32 步长；规划/预览/换绑/渲染统一，OOM 降级继续适配。
- 明确尺寸与高级覆盖保持严格校验，不默改用户输入；尺寸问题提前失败，旧任务恢复保持故事和已生成图片。
- 完整 **395 项测试通过**，真实 6 镜均通过 384×672、5 秒/124 帧/24 FPS 预检，无真实渲染提交。

## 2026-09-11 · C24 已取消短片无法删除

- 取消排队短片时立即移出本地队列、释放占用，保留其他任务顺序；导演/Bible/Shot/QA 和只读预检等待可及时取消，等清理结束再允许删除或重入。
- 删除失败区分后台停止中、生成中和未结算 ComfyUI 作业，保留 UNKNOWN 防重复执行与原有资产删除规则。
- 新增 9 项回归，完整 **377 项测试通过**。实际旧片占用已解除、数据保持；另一部短片正在生成，正式服务未重启，新代码待下次重启加载。

## 2026-09-11 · C23 整片重新生成入口

- 增加直接的「整片重新生成」按钮及明确确认，默认沿用剧本、提示词、参考图和可复用关键帧，可选择一起重做关键帧；沿用连续性与未结算任务保护。
- 旧成片独立展示版本选择、视频预览及下载；新任务运行、失败或再次重跑仍保留旧版本，旧镜头继续在重跑历史查看。
- 完整 **368 项测试通过**，隔离浏览器完成两种整片重跑与旧版切换；新页面已由 8000 服务提供，未提交真实模型生成。

## 2026-09-11 · C22 无秒数字段的帧数工作流

- 时长绑定允许 17n+5 等帧数对齐，新增可选固定生成 FPS，避免 MiniMax H3 被系统 16 FPS 改速；预览、生成、试跑、Resolver 与最终 JSON 统一时钟。旧绑定兼容，高级 FPS 与固定模型时钟冲突时明确拒绝。
- 页面说明 length/num_frames 可作为视频长度，支持任意 0～31 帧偏移和固定 FPS。实际工作流已绑定为 6.length、17n+5、24 FPS，5 秒自动输出 124 帧并沿用时间线裁剪。
- 全部 **367 项测试通过**，真实节点依赖预检与页面保存通过；仅修改所选 Workflow 记录，无真实生成。

## 2026-09-11 · C21 镜头与整片重跑

- 新增版本保护的重跑 API/表单，可选择单镜或全部启用镜头、仅视频或关键帧及视频；保留故事、提示词、参考图，归档旧产物供预览下载。连续依赖随前镜更新，独立镜头保持。
- 默认新种子且可沿用，高级用户固定种子仍优先；未知作业禁止重跑/时间线编辑，旧 retry 接口同步保护，不再先清空结果再报错。
- 完整 `make check` **361 passed**，隔离浏览器验证重跑范围、旧版本与断网恢复。真实短片未重新渲染。

## 2026-09-11 · C21 连接恢复

- 已受理任务断网后在等待期限内自动重连历史与产物下载，复用 prompt_id；WebSocket 恢复或继续 HTTP 轮询，保留最后进度。
- 页面断网保留数据、自动重新获取；超时/服务重启后的未知任务显示「重新连接并恢复」，不把状态未知当作远端失败，不自动重复提交。
- 新增 8 项恢复回归，针对性流水线验证通过；重跑能力与完整门禁继续在 C21 收尾。

## 2026-09-11 · C20 修复配置 5 秒却被显存策略拒绝

- 删除低显存 3 秒、快速质量 4 秒的隐藏时长限制；导演、预览、Resolver 与生成统一使用 workflow/用户显式上限，再校验模型参数范围、档位和帧数转换。显存继续约束图像尺寸与 batch，原 OOM 重试、分段和替代工作流保留。
- 本地旧失败记录及预览自动刷新预算中的时长字段，原故事和素材保持，避免旧的 3 秒预算持续阻止续跑。错误显示具体请求时长和配置范围。
- 实际短片 18×5 秒通过实时节点元数据与最终 JSON 时长校验，期间零渲染提交。完整 `make check` **342 passed**（72.06 秒）；正式服务已更新，实际旧预算和报错已修复为 version=61，原剧本与图片保持。

## 2026-09-11 · C19 失败后换工作流保留成果与恢复

- 修正 C18 只保护未渲染预览的遗漏：保存新绑定保留剧本、分镜、提示词、参考图、关键帧、已完成视频及批准记录。已有结果沿用，缺失部分或用户明确重试才使用新模型；旧作业及输入 JSON 不改写。
- 恢复入口只对旧版重置后的空短片提供，校验版本、未结算作业、历史来源、素材归属及文件；恢复故事但保留当前工作流，不自动生成。
- 新绑定续跑使用实时显存/节点预算，配置版本变化不再把已渲染故事退回不能继续的预览。保留已使用上传帧的授权，不放宽时长或 AI 参数约束。
- 实际短片已恢复至 version=56：18×5=90 秒、10 张参考图和首镜首尾帧，12 张图片正常加载，当前视频工作流保持。最终 `make check` **331 passed**（117.94 秒），16 项新增回归，正式服务已更新。验证细节见 C19 验收记录。

## 2026-09-11 · C18 渲染前更换工作流保留故事

- 未渲染的故事更换工作流时保留计划、镜头 ID/顺序/时长、视觉设定、提示词及用户编辑；仅更新生成配置和预览，重新等待确认。新时长或参数不兼容时原子拒绝，不清空故事。修改同一工作流配置后更新预览也复用已有内容。
- 专属 AI 参数按阶段绑定保留，旧模型值不复制到新 ID，原值在绑定历史；素材自动绑定、显存限制、高级覆盖及已渲染记录的归档重置继续适用。
- 按钮改为“保存工作流”，明确保留范围；干净的预览自动载入新版本，未保存编辑仍保留。完整 `make check` **315 passed**，11 项新增回归；隔离浏览器生成 10 秒夹具且无额外 LLM 调用。正式服务更新，用户现有 18 镜/90 秒及 version=29 保持。

## 2026-09-11 · C17 云端视频预览保存与实际分镜修复

- 修复规划阶段识别为 API 视频，保存/确认时却因临时标识丢失而误套 3 秒显存上限的问题。新预览保存执行方式快照；旧预览按已验证 duration 节点 ID/class_type 恢复，实时元数据优先，工作流版本和本地显存约束保留。
- 完整 `make check` **304 passed**，新增 11 项回归。正式服务更新，实际 90 秒短片通过现有 API 保存为 version=29，第 4、7、9 镜首尾帧/视频描述完整，全部重复 AI prompt 归档；只该记录改变，未提交真实生成。

## 2026-09-11 · C16 画面与旁白分离

- 画面 prompt 统一使用当前阶段的首帧/尾帧/视频/参考描述，Agent 不再在动态映射中重复生成这一角色；通用 AI 参数严格校验和高级覆盖保留。新增独立旁白脚本，不接入 TTS。
- 旧非空旁白和空动态 prompt 都不能覆盖画面，保存预览时归档重复字段。Bible 原始 AI 映射不再嵌入画面文本，预览 GET 保持只读。
- 完整 `make check` **293 passed**，新增 15 项回归。浏览器已验证画面编辑/保存/刷新及旁白独立展示。实际记录保存暴露另一处云端视频/显存校验错误，随后由 C17 修复并完成保存。

## 2026-09-11 · C15 空 AI 提示词导致无法开始

- 实际 90 秒/18 镜预览的第 4、7、9 镜，AI 动态 prompt 为空，覆盖了完整的分镜首尾帧提示词，导致开始按钮禁用且错误提示误指向时长。
- 预览与实际渲染共用空 AI prompt 回退，保留非空值、其他字段和显式用户覆盖；详情只读兼容旧预览。校验显示具体镜头/字段与时长差额，可定位；I2V 未使用尾帧不再作为必填输入。
- 完整 `make check` **278 passed**，新增 10 项回归。正式服务更新，实际页面开始按钮可用；原始分镜/提示词和 version 保持，没有重新调用模型或开始用户的真实视频生成。

## 2026-09-11 · C14 分镜预览与确认生成

- 网页创建默认先生成文字分镜与提示词，停在待确认状态；查看逐镜时长/动作/运镜，修改标题及正向提示词，保存或直接开始。确认前不提交 ComfyUI 渲染。
- 新增 preview 准备/编辑与 approve API，版本和时长校验、入口防绕过、工作流变更失效检查；编辑提示词不会被 AI 同角色参数覆盖，高级固定覆盖仍最高。保留旧记录/API、连续性、I2V 分支、QA、OOM 和 FFmpeg。
- 完整 `make check` **268 passed**，新增 16 项回归；隔离浏览器验证保存/刷新、总时长拦截、跨页冲突、自动保存确认及 10 秒 MP4，手机布局通过。正式服务已更新，原完成短片未修改；没有调用真实模型或重跑用户的短片。

## 2026-09-11 · C13 导演时长约束与分配修复

- 每批将单镜最短/最长秒数、允许和建议镜头数、建议时长同时写入 system 提示词、context 与动态 JSON schema，默认优先完整动作、避免不必要快切。默认争取 2.5 秒下限，短片/能力不足时自动下降，合法固定时长优先。
- 修复逐镜扣减余量导致前长后短的分配偏差，改为受上下限约束的同时比例分配，百分之一秒总和精确；均匀 4×2.5 秒保持不变。
- 新计划生效，已有计划和资产保持，显存上限、渲染时长适配与 FFmpeg 流程继续使用原契约。新增 30 项时长、schema 拒绝/纠正、固定覆盖和旧计划恢复回归；完整 `make check` **252 passed**（75.47 秒）。正式 8000 已更新，原完成短片未改动；没有执行新策略的真实模型生成。
- 同步 C12 已交付但任务文档仍标 IN_PROGRESS 的遗漏状态。

## 2026-09-11 · C12 工作流配置可用性

- 拆出输入与输出、模型与参数、检查与试跑三个页签。必需绑定直接可选，参数按节点分组并可搜索；常驻保存、取消修改、恢复导入值和字段错误定位。继续使用实际字段单一事实源，不恢复规则自动识别。
- 根据 ComfyUI 元数据区分云端 API / 未标记 API / 未确认，避免把本地部署等同于本地推理。修复 AUTOGROW 的 `values.a` 被误报缺少必填 `values`，不改节点或绑定。
- 完整 `make check` **222 passed**，新增 16 项回归；桌面与 390px 配置验收通过，正式服务已更新。两个真实工作流执行信息与动态输入检查已核对；没有提交真实渲染或切换用户默认项。

## 2026-09-11 · C11 动态模型时长修复

- 修复把 2.86 秒直接写入 MiniMax H3 整数 duration 导致提交前失败的问题；按已选 DynamicCombo 分支读取范围、步长、枚举及必填项，模型选择器返回正确 key。
- 视频时间线与实际渲染时长分离，2.86 秒请求合法的 4 秒视频，QA/实际尾帧/FFmpeg 使用原时间线范围；不可满足的约束提前失败，高级覆盖保持严格校验。
- 云端 API 视频不使用本地 3 秒时长上限，图片仍遵守显存策略。旧失败任务可保留分镜、Bible、参考图和首尾帧继续生成，已提交作业不改写。
- 最终 `make check` **206 passed**（51.15 秒），新增 12 项回归，Ruff/格式、前端 lint/typecheck/build 通过；真实 FFmpeg 验证 10 秒恢复导出。正式服务已更新，实际工作流依赖检查及 5 镜离线 patch 校验通过，没有重新提交真实渲染或检查远端 CI。

## 2026-09-10 · C10 模型超时在线配置

- 模型请求默认超时从 120 改为 600 秒；设置页支持自定义 1～3600 秒，保存后用于后续请求并持久化。旧文件缺少字段时自动继承默认值，环境变量为 `AD_LLM_TIMEOUT`。
- 导演、Bible、Shot 与视觉请求共用 `llm_timeout`；模型测试的总截止和等待提示读取同一配置，保留取消。ComfyUI 请求与 AI 工作流识别的独立限时保持不变。
- `make check` **194 passed**（49.13 秒），全部 lint/格式、类型检查和构建通过；浏览器验证默认值、自定义保存/刷新、文案更新和越界拦截。正式服务已重启并确认 600 秒生效；没有重复调用真实模型或执行远端 CI。

## 2026-09-10 · C09 导演与视觉模型测试

- 设置页新增“测试导演模型”和“测试视觉模型”，使用已保存配置执行最小 JSON / 测试图请求，显示耗时、结果与明确错误；缺少视觉模型时提示配置。
- 复用实际生成的 Provider，安全区分网络/超时、鉴权、权限、端点/模型、限流与格式错误。错误结构的上游响应也归为 LLM_INVALID_OUTPUT，保留一次 JSON 纠正；凭据和上游原文不回显。
- 测试总截止 120 秒，可取消；编辑、保存或离开页面会取消旧请求并清除结果。测试不会创建短片/渲染作业或修改配置。
- 本地浏览器验证成功、鉴权失败、未保存提示、取消与编辑期间取消；真实 `glm-5.3` 小请求 **7.82 秒通过**。未配置真实视觉模型，未重跑失败短片；完整工程门禁见 C09 验收记录。
- 最终 `make check` **187 passed**（63.30 秒），新增 22 项回归；56 个 Python 文件 Ruff/格式与前端 lint/typecheck/build 全部通过，正式服务已更新。

## 2026-09-10 · C08 短片工作流更换

- 短片详情显示实际使用的关键帧、参考图和视频工作流，可单独更换或读取最新默认项填充；保存不自动启动生成。
- 增加带 expected_version 的绑定更新接口；工作流类型、必需绑定和保留覆盖必须合法，运行中或 QUEUED/RUNNING/UNKNOWN 作业未处理时禁止更换。
- 更换后归档旧计划/Bible、镜头和成片引用并重置生成状态，按新能力重新规划；旧作业与素材文件保留。移除换走工作流的覆盖，以绑定版本隔离渲染缓存，旧记录无需迁移。
- `make check` **165 passed**（148.91 秒），新增 16 项回归；最终默认填充改为读取最新设置后，前端 lint/typecheck/build 再次通过。浏览器保存/刷新/取消与 4 秒夹具重新生成通过；正式服务已加载入口，原失败短片保持原状，未执行真实生成或远端 CI。

## 2026-09-10 · C07 默认工作流按钮修复

- 修复导入不再自动检查依赖后，“设为默认”仍以 `validation.valid` 为前提而一直禁用的问题；前端与现有后端的本地绑定校验契约对齐。
- 未保存编辑显示“保存并设为默认”，保存成功后才切换默认项；失败显示错误，生成前的依赖检查保持不变。页面明确默认选择仅影响新建短片。
- 完整 `make check` 为 **149 passed**（46.01 秒），新增 7 项 API 回归；浏览器验证图像/视频默认选择、编辑保存、失败拦截、刷新持久化，并在正式服务设置用户已导入的两个工作流为默认。
- 文档与截图同步更新；既有失败 Episode 仍使用旧 ID，未自动重跑，未执行真实模型或远端 CI 验收。

## 2026-09-10 · C06 快速导入、可选 AI 与实际字段表

- 移除上传时识别及导入后强制拉取完整 object_info；导入仅本地校验和保存，列表读取不重复分析，依赖检查单独执行。
- 删除 C05 节点规则推断；新增按需调用导演模型的只读 AI 建议接口，严格验证真实字段/类型/能力/占用关系，45 秒上限，可取消，无规则回退。
- 结果经用户确认后填入空项，已有手动配置优先；未配置、超时、非法结果均可继续手动导入。保留旧显式标签与既有数据，无迁移。
- 合并固定角色与动态参数两套表，只列 JSON 实际字段并在字段上设置用途、默认值和覆盖权限；文生图无预设参考图/首尾帧，准备区仅提示缺项。
- 完整 `make check` 为 **142 passed**（119.61 秒，无跳过），23 项新测试替代 8 项旧规则识别测试；原有 119 项保留，Motion fixture 改为显式用途标签。前端最后布局调整后 lint/typecheck/build 再次通过。
- 浏览器验证直接导入、AI 建议确认、保留手动绑定、取消/识别中导入，以及 3 个实际参数对应 3 行、无固定参数行。本地导入接口样本 20.52ms；正式服务已重启，未配置导演模型，真实 AI 识别与生成未验收。


## 2026-09-10 · C05 接入向导与浏览器收尾

- 上传先自动识别用途，默认页面显示中文输入/输出卡片，只要求确认歧义项；完整绑定、动态参数与帧数规则移入高级设置。
- 分开显示绑定、模型/节点依赖及试跑状态；缺失模型直接显示文件名与处理方式。I2V 不显示结束画面，自动补齐保留已有选择。
- 保存后同步服务端规范化结果，修复已保存仍显示待保存；试跑继承 profile 默认值，先校验依赖再进入渲染。
- 完整 `make check` 为 **127 passed**（44.74 秒，无跳过），Ruff/格式、ESLint、TypeScript、Vite 构建通过；隔离浏览器完成导入、歧义确认、重载、合成图片试跑和 390px 布局验收。真实本地服务已重启并核对依赖提示，未执行真实模型生成或远端 CI。

## 2026-09-10 · C05 自动识别与候选契约

- 新增无标签常见工作流识别，沿条件/素材连线区分用途；多个候选和未知帧数规则留待确认，保存节点与预览节点分开。
- 增加只读 analyze、保留手动选择的 auto-bind，以及独立 binding_issues；现有记录无需迁移。
- 8 项新增识别回归、22 项既有工作流/capability/流水线回归通过；完整门禁及浏览器验收在 UI 完成后执行。

## 2026-09-10 · C04 Capability Router 与完整收尾

- 新增 CapabilityRouter 基础层，按四类 default_capabilities 查询，保留显式 ID、旧媒体默认项和同能力回退；已创建 Episode 固定所选 ID。
- 创建、生成和低显存替代入口接入路由；保持现有 UI 流程，缺失/错误能力映射明确报错。
- 完整 `make check` 为 **119 passed**（56.50 秒，无跳过）；Ruff/格式、ESLint、TypeScript、Vite 构建通过。新增共 28 项用例，已有 91 项回归全部通过。真实模型和远端 CI 未执行。

## 2026-09-10 · C04 通用 AI-owned 动态参数

- Bible/Shot 输出增加按 workflow ID 隔离的 `ai_parameters`，schema 及服务端只接受当前 owner=ai 的键，严格执行类型、范围和枚举约束。
- 自定义非素材参数可声明 AI owner；经 ParameterResolver 到最终 deepcopy patch，用户高级覆盖优先，执行时重新校验节点约束，作业保留 AI 值与来源。
- 新增 21 项回归验证真实 patched JSON、Bible 与镜头值隔离、同名键隔离、非法输出拒绝、用户覆盖和用户锁；相关既有回归通过，前端 ESLint/TypeScript 通过。完整门禁待 C04 收尾。

## 2026-09-10 · C03 Workflow Capability 与参数归属

- 引入 media_type + 四类 capability，保留旧 type 输入/响应兼容；工作流默认项按能力记录，图生图通过文生图初始参考进入关键帧流程。
- 动态参数具备 owner、editable、override_policy；自动生成/规划/素材/系统值覆盖模板默认，高级用户覆盖最后应用，再检查能力和资源约束。
- AssetResolver/ContinuityManager 绑定真实资产，用户素材覆盖同步 Shot；防止模板占位文件、错误媒体类型和无授权的跨 Episode 资产进入渲染。
- 总片长 600 秒、新计划最多 240 镜集中在 core/limits.py；固定时长覆盖进入导演计划，OOM 强制保留较小尺寸和实际分段尾帧。
- 添加事务化版本迁移，保留已提交图、prompt_id、资产及旧 600 镜时间线；前端默认只需创作需求，高级表单显示 owner 和逐项覆盖。
- 最终 make check：91 passed，无跳过；图生图/I2V 夹具、60/90 秒计划、优先级、迁移回滚、原有 QA/Continuity/OOM/FFmpeg 全部通过。浏览器默认/高级生成及 390px 验收完成；真实模型质量与远端 CI 未执行。

## 2026-09-10 · C02 成片时长与长时间线

- 首页提供 5/10/15/30/60/90 秒预设和 1～600 秒自定义。前端从后端读取只读时长策略，计划、索引与时间线数量跟随策略推导。
- 将总时长与单镜工作流/显存预算分离；导演以最多 12 镜分批规划，携带整集目标和最近三镜上下文，支持批次间取消。
- 修复长视频逐镜帧数取整造成的累计漂移：按整条时间线对齐帧数，临时 PCM 音轨只在成片阶段编码 AAC。
- `make check` 为 77 passed；包含 600 秒规划、600 镜时间线、90 秒模拟生成和 600/598.8 秒真实 FFmpeg 合成。浏览器验证输入边界、1 秒夹具导出和移动布局，截图与限制见验收文档；未执行真实十分钟 AI 生成。

## 2026-09-10 · C01 在线配置

- 将模型端点、导演/VLM 模型名、密钥、ComfyUI 公网开关与运行限制搬到“连接与设置”，支持局部保存并立即生效。
- 在线覆盖以 0600 权限原子持久化；环境提供可选初始值，数据目录/网页来源仍为启动参数。密钥保留/替换/清除均不回显，切换端点须重新提供或清除凭据。
- 活动作业、取消未退出、UNKNOWN 状态阻止修改；保存失败不改变运行配置。README、接口、模块、运行和决策同步迭代。
- 完整检查 63 passed；浏览器验证保存、刷新、清除和连接检测；390px 无横向溢出。未调用真实模型或部署 ComfyUI。

## 2026-09-10 · T01 工程交付验收

- 增加 10/15 秒完整流水线与已知 prompt_id 恢复不重发回归；最终 `make check` 通过，42 项测试无跳过。
- 增加统一安装、开发、构建、运行和检查 Makefile；提供最小权限 CI 配置，远端 CI 尚未执行。
- 清理未实现的 Playwright 命令和依赖，保留可运行的隔离浏览器验收服务；保存真实工作台与合成夹具截图。
- 更新贡献指南、运行配置、任务状态、API 示例、模块/决策和验收记录；真实 ComfyUI/LLM/VLM 与视觉验收独立留在 T02。

## 2026-09-10 · G02 低显存工作流与连续性修复

- 低显存替代 profile 使用自身参数，不继承原模板节点 ID；共享语义角色继续传递分辨率、时长和首尾帧。
- 分段继承上一段实际输出尾帧；尾帧与视频 QA 采样按最终导出时长取样，避免继承被裁掉的多余帧。
- OOM 集成回归覆盖替代模板节点重命名；红/蓝真实视频验证裁切后的尾帧来自保留片段。

## 2026-09-10 · G02 质检证据保留

- 为候选图、关键帧和视频追加不可变 QA 历史，提供单集查询接口；定向重试保留失败结果与资产关联。
- 回归验证动作得分失败仅重做视频，关键帧不重复生成，历史同时包含失败与通过证据。后端 37 项测试通过。

## 2026-09-10 · D01 文档基线

- 固定 P0 + Phase 1/2 的整体 MVP，分离 Phase 3 与真实视觉验收。
- 建立任务、模块、API、开发运行、验收、决策和索引文档。
- 实测环境：Python 3.14.6、Node 22.23.1、FFmpeg 8.1.1；默认 ComfyUI 尚未取得有效响应。
- 本迭代仅完成文档基线，尚无代码实现验收。

## 2026-09-10 · B01 存储与配置基础

- 建立 Python 3.12 uv 锁定环境、配置/结构化异常、SQLite WAL versioned 聚合仓储和资产路径保护。
- ComfyUI 地址只允许配置来源；默认 loopback/LAN，阻止 metadata/link-local，公网需显式开关。
- 验证：`backend/.venv/bin/pytest -q`（backend 目录）3 passed；Ruff 检查通过。
- 用户明确先完成项目，不在本机部署 ComfyUI；真实 API 接入待整体交付后。

## 2026-09-10 · W01–W03 工作流与 ComfyUI 协议

- API JSON 图校验、角色解析、动态 schema、模型/字段错误、不可变 patch、秒数到模型帧数转换。
- 实现 HTTP 上传/下载、prompt/history/WS 进度、超时与按 prompt_id 取消；未知提交不自动重发。
- 内置 SD1.5 文生图、Wan2.1 FLF2V API 模板及依赖说明，profile 可独立编辑和替换。
- 结构与客户端测试使用固定夹具；不安装 ComfyUI、不宣称真实模型兼容/质量验收通过。

## 2026-09-10 · A01 导演与模型适配

- 四个 schema 受限的 Agent；统一 JSON Provider、图像输入、token/call 统计、拒绝和无效输出分类。
- Director 强制 1 秒下限、workflow 上限与总时长；Bible/Shot 继承单集设定；QA 根据实际图像得分分流重试。
- 支持显存/质量模式预算，High 规划两个候选；模型未配置时返回配置错误。
- OpenAI-compatible 层采用 Chat Completions `json_object` 兼容模式 + 本地 Pydantic 强校验；不把 JSON mode 当远端 schema 保证。[官方 API](https://developers.openai.com/api/reference/resources/chat)

## 2026-09-10 · M01 本地媒体链路

- 资产按单集分区、服务端 UUID 命名并记录 SHA256/大小/媒体元数据；检查图片内容与扩展名。
- FFmpeg 参数数组调用、进程超时/取消清理、实际视频尾帧提取、尺寸/FPS/音轨规范化与 MP4 合成。
- 真实 FFmpeg 测试混合 320×180/180×320、16/24 FPS、有声/无声片段，验证 2 秒 H.264 成片、音轨和可解码尾帧。

## 2026-09-10 · G01/G02/API 单集流水线

- FastAPI 实现工作流库/试跑、设置检查、单集/镜头、进度 SSE、资产上传预览、成片下载与定向重试。
- 单 worker、持久化步骤、不可变 RenderJob 快照、未知提交核对/恢复；取消中的旧任务结束前禁止再次入队。
- 参考资产→首尾帧→VLM QA→视频→采样 QA→实际尾帧→FFmpeg；连续镜头依赖与时间线变化显式失效。
- 使用仅存在于 tests 的 ComfyUI/LLM 固定夹具及真实 FFmpeg，完成 5 秒双镜头生成、视频单独重试（关键帧和其他镜头保留）、工作流上传试跑、错误边界。
- 本地 27 项测试通过；依赖中有 Starlette/httpx 和 AnyIO 的弃用提醒，不影响当前结果；真实 ComfyUI/VLM 仍未验收。

## 2026-09-10 · U01 Web 创作工作台

- React/TypeScript/Vite 实现创作、短片库、Episode 时间线、工作流库/绑定/参数/试跑、连接与设置。
- 接入首尾帧/视频预览、成片下载、镜头重试、拖动/按钮排序与启用、UNKNOWN 作业核对和人工结论。
- 响应式深色布局、表单 label、禁用状态、明确错误提示；密钥不进入浏览器。
- `npm run build`、`npm run lint` 通过；浏览器核对创建页布局、时长切换、输入后按钮状态、工作流绑定与设置页，无应用控制台 error。

## 2026-09-10 · T01 失败恢复与预算回归

- 修复提交响应 JSON 损坏、缺 prompt_id、5xx 时的 UNKNOWN 分类；已完成但无输出时立即返回可诊断错误。
- 角色解析支持数字后缀；重复输出角色保持待绑定；渲染缓存签名纳入绑定/输出/capabilities。
- 队列入队接口切换到事件循环执行；显存降级与 QA 共用重试预算，避免嵌套超额重试。
- 新增取消期间重复入队/删除保护、重启保存 prompt_id/资产、OOM 预算 1 失败与预算 2 分段完成回归。
- 当前后端 36 项测试通过；浏览器在隔离依赖夹具中完成 5 秒生成、下载链接和视频单独重试，重试后成片资产 ID 更新。390px 设置/创作页无横向溢出。
## 2026-09-10 · C04 视频能力分支收尾

- I2V 只生成/绑定首帧，关键帧 QA 使用实际单帧；FLF 保持首尾帧生成。
- I2V 转场重试和 OOM 分段不创建多余尾帧/中间目标帧，实际视频尾帧继续用于连续性。
- 流水线与 capability 集成回归 21 passed，另新增的 I2V 转场失败重试回归 1 passed；完整门禁待 C04 收尾。
