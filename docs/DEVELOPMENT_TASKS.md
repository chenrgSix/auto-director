# 开发任务与交付目标

## 当前实现 C63：多参考连续镜头制作

冻结目标和门禁见 [连续镜头制作](CONTINUOUS_PRODUCTION.md)。用户已授权按该方向实现；参考确认后按连续组生成，首尾关键帧退出新模式的必经路径。同步创作包/API、网页和 MCP，第一版明确整组重跑。

状态：**IMPLEMENTING**。C62 已独立验证更新后的 Director 两段多图与 AV 接续；该证据不代表项目适配已经完成。

## 当前验证 C60：Ref2VA 接入与段间运动上下文

目标：用户已补齐 Ref2VA INT8 主模型和 Ref2V Turbo 4-step LoRA，接入可选工作流并用短片段验证多图参考及导演台段间引导。

- [x] 提供主模型/LoRA 配套的多参考视频 API/profile，保留首帧控制、原生音轨和可选参考输入。
- [x] 通过实际节点校验和项目试跑；用同一人物、场景、道具生成两段接续动作，检查边界和中段。
- [x] 记录真实耗时、失败、实际导出帧数和视觉限制，保留旧片与默认配置。

范围：单镜工作流进入项目库；导演台多段执行先作为可复现的独立验收工作流。创作包中的连续镜头组、组级缓存和局部重跑仍需后续契约设计，不能将插件实测等同于项目已经完成整组编排。

状态：**VERIFIED（单镜接入、原生 Motion Context 实测）/ PARTIAL（动作连续性）**。当前导演台插件调用官方节点时发生位置参数错配，未进入采样；替代为原生 Ref2VA + 已安装 Motion Context。两段合计 260 帧/10.833 秒，边界未见明显换人换场，但前段提前松手导致接缝附近停顿，连续推碗未通过。`make check` 973 passed，随后 R2V 提示词语义修复另有 38 项回归；实际服务已验证不再添加错误的首帧参考头。详见 [C60 验收](ACCEPTANCE.md#2026-09-14--c60-ref2va-与运动上下文)。

## 当前验证 C59：三图参考真实模型验收

目标：用户授权代为运行独立《雨停之前》三镜验证，按参考/关键帧/视频阶段检查真实输出；保留旧片和默认配置。

- [x] 建立人物、空场景与面碗参考并实际查看；道具局部修订一次。
- [x] 检查特写构图、送碗位移、反打的道具和空间；记录首镜失败、后两镜动作与连续性通过。
- [x] 检查真实提交图、渲染耗时与失败记录、实际视频边界和中段，完成项目修正、局部重跑与独立对照导出。

状态：**VERIFIED（真实执行与本地工程门禁）/ PARTIAL（视觉）**。11 秒规划（2/5/4 秒），实际拼接约 11.959 秒。完整 `make check` 969 passed；五类参考、实际三图输入、MCP 分阶段复核和局部重跑已验收。无人特写默认路径三次均在中途插人；整段简化提示的独立对照保持面碗，不能据此宣称默认路径已解决。第一镜 needs_changes、后两镜 passed 已写回，成功对照另行拼接并保留来源；见 [C59 验收](ACCEPTANCE.md#2026-09-13--c59-三图参考真实模型验收)。

## 当前迭代 C58：制作前画面确认与多图参考

冻结目标见 [制作前画面确认](PREPRODUCTION_REVIEW.md)。

- [x] 清理参考图提示、共享道具与有序多图参考。
- [x] 参考图及逐镜关键帧确认、修订、网页和 MCP 接管。
- [x] 隔离回归、完整门禁、浏览器与真实节点依赖验证。

状态：**VERIFIED（本地门禁、官方 MCP、隔离浏览器）**。完整门禁 958 passed，后续针对性回归 25 passed；保留旧片和默认配置，真实多图画质与显存单独验收。

## 当前迭代 C57：项目级镜头参考与连续性

冻结目标及验收见 [镜头参考与连续性](SHOT_CONTINUITY.md)。覆盖普通 API 与创作包/MCP 的共同执行链路，不改写当前案例。

- [x] 结构化镜头约束、按镜头选择参考及预检。
- [x] 实际边界复核、素材版本保护及 MCP 局部重跑。
- [x] 前端呈现、回归、完整门禁与隔离浏览器。

状态：**VERIFIED（完整本地门禁 952 passed、官方 MCP 客户端与隔离浏览器）**。8000 已加载，1,870 条原记录保持，未改写或重跑用户旧片。真实模型衔接质量与 CI 尚未验收，详见 [C57 验收](ACCEPTANCE.md#2026-09-13--c57-项目级镜头参考与连续性)。

## 当前迭代 C56：完整片段拼接

目标：剧本时长只作规划和模型输入参考，成片按实际生成的完整音视频拼接；为当前 60 秒剧本重新导出，保留旧成片。

- [x] 删除按计划秒数截尾和过短重拍；QA、优化取样、连续性及 OOM 分段使用完整视频。
- [x] 页面区分参考/实际时长，重新拼接成功后归档旧成片，失败不移除旧版本。
- [x] 真实 FFmpeg 尾帧/尾音、长片、短输出、重新导出保护回归及全量门禁。
- [x] 正式服务加载后只重拼现有 20 镜，验证音视频、旧资产与零模型重跑。

状态：**VERIFIED（完整本地门禁 937 passed、真实旧片重拼、浏览器播放加载）**。新片 65.796 秒，20 段 1579 帧完整保留；旧成片/191 个资产与 65 个渲染作业均保持。未执行 CI 或重新生成素材质量验收。

## 当前迭代 C55：异步视觉复核与显式开关

目标：建议式复核不再等待视觉模型后才生成下一镜，用户可明确选择是否调用视觉模型，保持故事、媒体、严格质检和技术检查契约。

- [x] 单个后台复核 Worker、持久化 pending/running/completed/failed/skipped 状态；成片可先完成，结果随后更新。
- [x] 新建页显示独立开关；已有短片通过 qa-policy 的可选 qa_enabled 字段控制，advisory 允许生成中切换，strict 切换沿用空闲保护。
- [x] 关闭取消未完成请求，旧结果保留；重启恢复待检任务，结果按任务 ID、实际素材、提示目标及模型配置校验，拒绝过期写回。
- [x] 回归、全量门禁（928 passed）、隔离浏览器和正式加载；不自动复检旧片或重跑旧媒体。

状态：**VERIFIED（本地门禁、隔离浏览器与正式流水线重叠执行）**。用户 60 秒成片已完成，最后两镜复核不再阻塞成片。无数据库表迁移，新 JSON 字段可选，旧记录不自动补检。创作包的人工复核授权仍独立保留；不代表视觉或音频质量验收通过。

## 当前迭代 C54：服务常驻与中断恢复

目标：修复本地 8000 服务依赖临时会话、退出后无人拉起的问题，恢复用户当前短片的原远端作业，保留剧本与全部旧媒体。

- [x] 新增用户级 launchd 安装、启停、状态与持久日志；单进程、固定虚拟环境与数据目录，避免重复安装中断任务。
- [x] 常驻配置/所有权/端口保护回归与完整项目门禁（913 passed）。
- [x] 正式服务启动与受控退出拉起验证；恢复第 12 镜原 prompt，核验旧资产保持。

范围：不改 QA/生成编排，不自动重发未知作业，不把自动拉起当作已查明退出原因。状态：**VERIFIED（本地门禁、正式服务自动拉起与原作业恢复）**。用户整片仍在继续生成，不代表整片或视觉质量验收完成。详见 C54 验收记录。

## 当前迭代 C53：创作包与 MCP

冻结目标：用户自己的 Codex 通过 MCP 创作并交付版本化创作包；AutoDirector 校验、预览确认、制作并回传反馈。完整包不需要文字模型 API，操作/关闭 Codex 不影响服务与受理制作。

- [x] 创作项目、不可变版本、导入导出、约束/schema 与校验（后端）。
- [x] 零文字模型的预览制作、版本/配置保护与幂等提交（后端）。
- [x] 本地 MCP 与连接说明，复用网页业务服务。
- [x] 页面工作台、校验、版本及制作反馈。
- [x] 针对性回归、官方 MCP 客户端和完整 make check。
- [x] 隔离浏览器：新建/导入/冲突保护/窄屏、实际文件下载往返、页内确认与网页制作按钮。

状态：**VERIFIED（第一期本地交付、官方 MCP 协议与隔离浏览器）**。见 [C53 目标与门禁](CREATION_PACKAGES.md) 和 [验证记录](ACCEPTANCE.md#c53-分阶段验证)。保留用户 README 和未跟踪源设计文档；未重启现有 8000，不自动生成真实用户素材或变更用户 Codex 配置。真实 Codex 创作质量、真实模型画面、远端 CI 与正式服务加载分别验收。

## 当前迭代 C52：自动编写 H3 原生声音提示词

目标：把 C51 验证的中文对白/画外旁白格式接入两套 codex H3 视频工作流的新分镜生成。以可编辑视频提示词为唯一执行来源，保留旧故事、素材及高级覆盖。

- [x] 工作流显式声明 `audio_prompt_format=minimax_h3`，默认 `none`；单镜、批次与重跑共享受约束提示词契约，图像提示词保持纯视觉。
- [x] 视频提示词包含画面、说话人/原文台词、环境声及音乐说明；视觉 QA 优化保留声音段；有台词的 OOM 不盲目分段重复整句。
- [x] 更新两套导入配置、页面说明及正式配置；回归、完整门禁与有限真实 Agent→ComfyUI 验证分别记录。

范围：不增加独立 TTS、配音队列或旧片补声。声音及口型仍由 H3 生成，视觉 QA 不代表听力验收。旧分镜只有用户选择重新生成提示词时才采用新规则。

状态：**VERIFIED（本地门禁、页面保存、有限真实声画链路）**。完整门禁 875 passed；最后的帧引用时钟修正另有 43 项回归通过。真实 Agent 两镜经一次格式纠正生成合法提示词；I2V 对白 / FLF 旁白约 76 / 81 秒，独立 ASR 原文匹配。8000 已加载，原 971 个素材及所有故事保持，详见 C52 验收。

## 当前验证 C51：H3 中文人物对白

目标：用当前 8GB / Turbo 8 步 / 原生音频的首帧视频工作流，验证 5 秒中文人物对白及闭嘴画外旁白对照；明确区分台词传入、真实音轨、转写准确性与嘴部时序证据。

- [x] 生成独立虚构成年人物首帧，按 H3 官方说话人和台词格式完成对白与画外旁白试跑；没有修改现有故事或工作流执行配置。
- [x] patched JSON 含完整中文台词和音频连线；独立 ASR 未提供参考文本，正确转写两句。对白嘴部变化明显，旁白 21 个抽帧保持闭嘴；声音偏轻，严格时间安排与音素级口型同步尚未通过。
- [x] 两段生成耗时 78.73 / 75.07 秒，证据见 C51 验收。原 968 个素材和故事/配置保持，仅增加三项试跑与对应素材、更新两条工作流的最近试跑元数据；自动对白提示词传递仍未实现。

## 当前迭代 C50：恢复 codex 视频原生音频

目标：为现有两套 `codex-H3` 首帧/首尾帧视频工作流补齐原生音频解码与 MP4 输出，沿用 8 步采样、尺寸和帧数；保留用户故事、提示词、默认项及所有旧素材。

- [x] 更新 API 图、导入配置与原生画布；音频从同一次联合采样结果解码，连接现有视频输出，不增加第二次采样或独立 TTS。
- [x] 增强两类能力连线及真实 FFmpeg 音频内容/时间位置断言；完整 `make check` **851 passed**。实机同份采样补音频解码/保存 2.21 秒，实际应用首帧/首尾帧完整有声作业 73.51 / 77.49 秒，10 秒拼接保声通过。
- [x] 两条现有 codex 工作流和对应 ComfyUI 画布已原位更新，ID、绑定、覆盖与默认项保持。原 1,454 条记录仅更新两条工作流，966 个原素材文件全部保持；新增两项有声试跑作业及素材。详见 C50 验收。

范围：只补回视频模型原生音频，不重跑用户整片、不新增旁白合成或配乐系统；旧无声视频保持原文件。

## 当前迭代 C49：分镜提示词小批次准备

目标：在现有单 Worker 中按预算每次准备 1～3 个相邻缺失镜头，批间沿用末镜连续状态，减少重复上下文和串行请求次数；不改变镜数、时长、故事、渲染队列或旧成果。

- [x] 新增可配置的提示词批次大小，默认上限 3；复杂上下文自动缩小，1 恢复单镜模式。共享原 Shot 指令、AI 参数与动作时间校验。
- [x] 固定每批镜号及顺序，完整校验后原子落库；源快照、任务身份与取消状态在提交时校验。恢复只补缺失项，失败不自动无限拆批重试。
- [x] 显示只读准备结果、批次等待与耗时/调用统计，统一后端准备阶段进度；全部准备完成前不可编辑/批准，不提交渲染。
- [x] 回归覆盖 95/240 镜、四类能力、连续状态、过期/取消/重启、参数非法、用户覆盖和部分恢复；完整门禁 851 passed，隔离浏览器和有限真实模型对照通过（三镜 164.86 → 67.77 秒）。
- [x] 2026-09-13 用户生成完成后，确认应用与 ComfyUI 队列空闲，重启正式 8000 加载后端，默认批次上限 3。1,454 条记录、966 个素材文件及在线配置跨重启完全保持，原 95 镜 / 300 秒成片仍可访问；详见 C49 验收补记。

范围：不加入跨镜并发、不改变 QA/OOM/UNKNOWN/FFmpeg、现有单镜重跑逻辑或已保存提示词。性能收益以真实对照为准，默认模式如无收益可回退为单镜。

## 当前迭代 C48：工作流计算路径优化

目标：在相同素材、提示词、种子、尺寸和视频时长下比较采样与解码路径，针对 8GB 显存改进工作流本身；把经实测有效的调整写回 `codex` 工作流。

- [x] 记录逐节点耗时及显存可用量，区分缓存命中、模型加载、采样和解码。
- [x] 验证图生图的匹配 Turbo 采样、图片/视频解码粒度，以及现有节点可用的显存优化；不以缩短时长或降低尺寸充当同条件性能提升。
- [x] 首尾帧同条件采样 73.94 → 53.14 秒，视频保留原生 Kitchen attention；Z-Image 使用整图解码；图生图保留基础 12 步。四份原生画布保存到 ComfyUI，三个实际变更的 AutoDirector 工作流原 ID 更新。
- [x] 新增画布/API 连线与控件值一致性测试，覆盖真正连入采样器的 attention 和高级回退覆盖；相关测试 **63 passed**。完整实机记录与验证限制见 C48 验收。

## 当前迭代 C47：8GB 实机工作流包

目标：复用用户 ComfyUI 已安装模型，准备以 `codex` 开头的四类能力工作流，显式绑定输入、输出、owner 和 H3 帧数时钟，完成低分辨率串行试跑。

- [x] 读取真实节点/模型清单，新增可复用 API 图与完整导入配置，四套绑定与依赖校验通过。
- [x] 文生图选择 Z-Image INT8；图生图匹配 FL2VA 权重；视频使用匹配 Turbo 8 步 LoRA 和分块解码。
- [x] 四种能力真实 ComfyUI 试跑通过；两段视频均为 640×384 / 24 FPS / 124 帧，旧记录及默认项保持。59 项相关测试和 30 组真实节点约束组合通过；详见 C47 验收。

范围：不安装模型/节点，不修改生成架构、旧工作流、默认项或已有短片。工作流包按需导入，删除后不会因重启恢复。

## 当前迭代 C46：生成素材前 AI 审查剧本

目标：导演完成剧本后，先独立审查用户意图、剧情因果、角色/场景连续性、单镜动作与时长、首尾状态及转场。审查先于 Bible、图片和视频；仅进行一轮有界修正，不循环重写。

范围：受约束的逐镜文字修正、审查摘要/遗留问题、原稿与修改原因留存、审查阶段进度、预览展示。保持镜头数量/顺序/时长、创作要求与工作流覆盖；无法局部修好的实质问题要求人工确认后才渲染。网络/格式失败保留剧本，重试只恢复未完成审查。旧计划和旧媒体不自动重审或改写。

验收：审查顺序、修正进入下游、非法/越权修改拒绝、取消/失败/重启恢复、审查缓存、待处理问题阻止自动渲染、显式确认、历史兼容及全量门禁、隔离浏览器。状态：**VERIFIED（本地门禁、隔离浏览器、真实模型审稿与服务保持）**。完整 make check **786 passed**，新增 29 项回归；真实模型一次修正两项预设矛盾。正式 8000 已更新，原 265 条记录、126 个素材及配置散列保持；未执行真实视频质量验收。

## 当前迭代 C45：单镜内部动作节奏

目标：新生成的分镜提示词明确“哪段时间做什么”，按剧情拆分同一主动作，避免等长切块或堆砌新动作。4 秒及以上镜头生成 2–4 段，较短镜头允许省略；时间从 0 连续覆盖实际单镜时长，无重叠、空档或越界。

范围：Shot Agent 受约束输出、视频提示词编译与预览、修改时长后比例调整、OOM 分段的局部时间线、AI 优化保留时间约束。时间段合并进现有可编辑视频提示词，避免两份相互冲突的配置；高级覆盖优先，旧分镜与素材不自动改写。

验收：5 秒分段、短镜省略、小数时长、非法时间线纠正与拒绝、真实 Provider schema、预览编辑与最终工作流 patch、分段裁切与覆盖兼容。状态：**VERIFIED（本地门禁、隔离浏览器、真实模型提示词及服务保持）**。完整 make check **757 passed**，新增 38 项回归；真实导演模型一次返回合法分段。正式服务已更新，265 条记录、126 个素材及配置保持；未执行真实视频节奏/画质验收。

## 当前迭代 C44：基于实际画面的提示词优化预览

目标：把画面复核问题接入可确认的定向修正，减少盲目重跑。观察当前视频与输入关键帧，生成修改原因、提示词前后对比及重跑范围；用户确认后才生成。保留故事、顺序、时长、视觉设定、高级覆盖和旧视频。

范围：三类画面提示词的受限编辑；视频问题复用关键帧，尾帧问题只失效尾帧与视频，首帧修正也保留未修改的尾帧，连续后镜提前列出。五点按时间顺序抽帧，明确采样局限；低置信度/模型能力限制可建议人工处理，不强行改写。初次编写约束单镜动作量。优化预览不改 Episode、不提交渲染；确认受版本、工作流配置及未结算作业保护，每次确认执行一次语义修正，技术重试沿用原预算。

验收：预览零渲染与数据保持、真实 QA 意见/图像进入优化上下文、非法/越权改写拒绝、提示词进入最终 patch、必要范围重跑、旧资产可读、I2V 无尾帧、过期/取消/恢复、全量门禁与隔离浏览器。状态：**VERIFIED（本地门禁、隔离浏览器、真实模型预览与服务保持）**。完整 make check **719 passed**（218.70 秒），新增 42 项回归。真实第 4 镜返回可确认优化方案，原 264 条记录、126 个素材及配置保持；未执行真实视频重跑或宣称画质验收通过。

## 当前迭代 C43：短片页面以视频为主

范围：播放器和分镜预览置于主区域；镜头视频先于首尾帧/质检长文；设置、提示和作业记录默认折叠。桌面播放器与镜头列表并列，窄屏先播放器后列表；阻断错误与恢复入口仍可见。修复质量/质检面板同 key 导致轮询重复插入的缺陷。仅调整展示与页面操作入口，不改变任务、数据或生成策略。

验收：完成、生成中、待审和失败状态的布局顺序；镜头选择、待复核定位、设置展开、预览编辑及窄屏无溢出。状态：**VERIFIED（本地门禁与浏览器布局/交互）**。677 项测试通过；桌面与 390px 浏览器验证播放器顺序、折叠区、镜头定位及编辑保持，轮询面板各 1 份。仅更新前端，后台生成不重启。

## 当前迭代 C42：整片优先与画面待复核

范围：新增 advisory/strict 质检策略。页面新建默认先完成整片；旧数据与 API 缺省继续 strict。advisory 每镜单候选、视频完成后一次语义 QA，未达标或视觉服务异常保留视频并标记待复核，继续合成；执行、素材、时长、取消和不确定提交保护保持。策略切换保留分镜与资产，已完成镜头不重跑。相同素材与目标的已完成 QA 可恢复复用，主动重跑清理当前复核状态并保留历史。

验收：两种策略分流、多镜完成与复核标记、模型异常与技术故障边界、high 单候选、恢复缓存、API 并发/旧数据、预览编辑保持、浏览器和完整门禁。状态：**VERIFIED（本地工程与服务续跑，真实全片仍在生成）**。完整 make check **677 passed**（182.47 秒），49 项新增回归；Ruff/格式及前端 lint/typecheck/build 通过。重启前后 181 条记录、72 个素材文件和配置散列保持。当前片保留故事、所有原绑定及前两镜视频，切换 advisory 后从第三镜现有帧提交视频作业；未重画图片。真实第三镜再次出现语义 QA 失败后保留为待复核，已自动进入第四镜（完成 3/10 镜）。浏览器控制连接不可用，未完成本轮浏览器交互和真实全片视觉验收。

## 当前迭代 C41：当前图像独立质检与图生图纠错输入

范围：QA 只读取本次图像和当前已审核目标，排除历史评语、错误、内部对比指标与全片角色目录。图生图纠错将对应失败帧作为编辑输入，使“修正这张图”的建议与实际素材一致；首轮仍按原参考策略，文生图不虚构素材绑定，高级覆盖和 UNKNOWN 恢复保持。保留既有剧本、图片和视频，不通过降低门槛掩盖真实画质问题；模型瑕疵评分改用明确的严重度命名及数值说明，兼容旧输入/API 输出。

验收：干净/带历史镜头产生相同 QA 上下文；连续失败使用各自最新失败图，首帧合格则不重画；I2V/文生图/高级参考覆盖及断线恢复回归；实际图与代码链路验证分别记录。

状态：**VERIFIED（本地工程、真实单帧编辑/质检与服务数据保持）**。完整 make check **628 passed**（169.35 秒），新增 36 项回归；Ruff/格式、前端 lint/typecheck/build 通过。真实单帧编辑去除黑色条纹，同图在明确瑕疵严重度字段后达到 standard 门槛；仍有细节差异，不代表全片验收。空闲更新 8000 后，144 条记录、54 个素材文件及在线配置散列完全保持，原短片 FAILED/version236 和全部原绑定不变，未启动整片。

## 当前迭代 C40：镜头提示隔离与质检定向纠错

范围：当前镜头渲染采用已审核的阶段提示，不再附加全片角色生命周期和场景风格；无独立 negative 输入时将限制传入自动正向指令，用户高级覆盖优先。QA 输出结构化失败帧与修正建议，只重画失败帧；耗尽重试后继续生成隔离旧失败作业，已受理 UNKNOWN 作业仍按原快照恢复。保留剧本、提示词、合格素材、成功视频与历史文件，不自动续跑真实任务。

验收：破壳镜头不混入厨师/成鸡/炉火设定；限制进入最终 patched JSON；尾帧失败不重画首帧，纠错建议用于新图；继续后作业/seed 更新且旧素材可读；I2V、用户覆盖、旧 QA 输出、UNKNOWN/OOM 与全量门禁通过。

状态：**VERIFIED（本地工程、真实工作流无提交复核与服务数据保持）**。完整 make check **592 passed**（163.59 秒），新增 43 项回归；Ruff/格式、前端 lint/typecheck/build 通过。真实 MiniMax 图的 edit_instruction 已包含限制且不再混入全片设定，未提交渲染。确认空闲后更新 8000，123 条记录、47 个素材文件及在线配置散列保持；原短片 FAILED/version162 与第一镜视频保留，未自动续跑。

## 当前迭代 C39：提前校验实际工作流与统一约束说明

范围：素材生成前检查接下来会执行的工作流，上传前检查最终参数及全部素材；使用自动值与高级覆盖后的图，避免模板旧默认误报。AI schema 与动态参数页面同步显示 step 及下游约束，仍以服务端校验为准。保留已提交作业的原快照恢复、未使用工作流不阻塞续跑、故事与资产不失效；不猜测任意数学/自定义节点行为。

验收：缺节点/模型、固定参数或素材非法均在相应外部副作用前失败；合法用户覆盖替代失效默认值；AI 下游规则严格校验；原首尾帧与 UNKNOWN 恢复回归；完整 make check、隔离页面检查及服务数据保持验证。

状态：**VERIFIED（本地工程、隔离浏览器与服务数据保持）**。完整 make check **549 passed**（152.01 秒），新增 27 项回归；最终补充参数定位后前端 lint/typecheck/build 再次通过。页面验证约束展示、688 保存拒绝/672 保存成功、下游错误定位到可编辑源字段。用户任务自行停止于第二镜视觉 QA 后，确认无未结算作业再重启 8000 加载优化；120 条记录、47 个资产文件及在线配置散列完全保持，第一镜视频保留，未自动续跑。

## 当前迭代 C38：整数节点传入尺寸的下游约束

范围：修复 PrimitiveInt/PrimitiveFloat 的 value 经输出 0 直连节点时遗漏消费者约束的问题；自动尺寸同时满足源参数及所有消费者的 type/min/max/step/enum，显式非法覆盖仍拒绝。只处理已知数值透传，不猜测数学节点公式。沿用实时刷新、恢复和素材复用，无故事重规划或资产删除。

验收：旧 profile 实时刷新后 384×688 自动适配为 384×672；多个消费者保留各自 step 起点；非法覆盖/无合法尺寸在提交前拒绝；旧失败片续跑复用首尾帧，旧 Job 和文件保持。完成定向回归、完整 make check 与真实工作流无提交复核后更新本地服务。

状态：**VERIFIED（本地工程、真实工作流无提交复核与服务保持）**。完整 make check **522 passed**（150.55 秒），新增 18 项回归；真实 MiniMax 图仅高度节点从 688 改为 672，依赖检查通过。空闲重启 8000 后 97 条记录、36 个资产及在线配置散列保持；未自动续跑真实任务。

## 当前迭代 C37：已有短片切换质量模式

范围：详情页新增质量模式编辑，允许空闲、已取消或已完成短片在快速/标准/高质量间切换。保留剧本、时长、提示词、视觉设定、参考图、已绑定素材及旧作业；保存不自动生成。候选数、质检门槛及重试次数跟随模式默认值，已有画面尺寸沿用。清理未完成镜头的旧执行游标并隔离旧作业版本，禁止活动/未结算作业或过期版本改配置。

状态：**VERIFIED（本地工程、隔离浏览器与部署保持）**。完整 make check **504 passed**（154.94 秒），新增 17 项回归；最终前端补充分镜未保存编辑保护后 lint/typecheck/build 通过。浏览器验证切换标准、保留并保存未保存文字、重新打开保持且不入队；正式服务已加载，89 条记录、34 个资产散列及在线配置保持。用户尚未选定新模式，未代选或自动续跑真实任务。

## 当前迭代 C36：按剧情决定镜头数量与节奏

范围：删除默认规划中“优先最少镜头、接近上限等长”的引导；由 Director 按动作、情绪和机位选择镜头数量/时长，2.5 秒仅作软提示，硬约束仍为至少 1 秒、有效工作流上限、准确总时长及最多 240 镜。长片分批预留拆镜空间，并为后续批次保留必要镜头额度。保留用户明确的固定单镜时长、原计划与重跑语义，不自动重写已有短片。

验收：30/60/90 秒可保存模型提供的不同镜头数量和非等长节奏；短镜、固定时长、600 秒/240 镜边界、非法输出纠正及旧片续跑有回归；完成完整 make check，空闲时更新本地服务并核对原数据。

状态：**VERIFIED（本地工程与部署保持）**。完整 make check **487 passed**（143.72 秒），新增 14 项回归，Ruff/格式、ESLint、TypeScript、构建通过。隔离流水线验证 8 镜非等长预览、保存、确认和真实 FFmpeg 导出 30 秒。正式 8000 已加载，54 条记录、21 个资产散列与在线配置跨重启保持；未提交真实模型或渲染任务。

## 当前迭代 C35：重跑时重新生成镜头提示词

范围：重跑新增 prompts 选项，重新调用 Shot Agent 编写所选镜头的提示词/AI 动态参数，再生成关键帧和视频。保留分镜故事、顺序、时长、Bible、参考资产及旧版本；后续连续依赖只重做必要素材，全部选中的镜头均重写提示词。高级覆盖与定格选择继续优先，失败可继续生成，旧提示词可在历史查看。兼容原两种重跑模式，不自动重跑真实短片。

状态：**VERIFIED（本地工程、隔离浏览器与部署保持）**。完整 make check **473 passed**（142.36 秒），新增 13 项回归，Ruff/格式、ESLint、TypeScript 和构建通过。浏览器验证整片重写提示词、旧提示词查看和旧成片下载；正式服务已加载，最新 54 条记录与 21 个资产散列跨重启保持。未自动重跑真实短片。

## 当前收尾 C34：工作流 AI 识别共用模型超时

范围：移除独立的 45 秒识别上限，使用「导演与视觉模型」在线 llm_timeout 配置；每次请求读取当前值，覆盖模型请求及 schema 纠正的总等待，保留取消、断线清理与手动绑定。前端不另加较短固定超时，同步更新说明。完成后空闲重启正式服务，一并加载 C33，保留所有原数据。

状态：**VERIFIED（本地工程与实际部署保持）**。最终完整 make check **460 passed**（131.50 秒），Ruff/格式、ESLint、TypeScript 和构建通过。相关 75 项含在线超时更新、超时取消及断线取消；正式服务已加载，当前等待 600 秒，203 条最新记录和 104 个资产散列跨重启保持。未提交真实 AI 识别或渲染。

## 当前修复 C33：首尾帧目标与重复校验

范围：当前渲染目标优先于全片设定及上一镜状态；参考图仅保留身份，不锁定姿态、位置和出场数量。首尾帧视频在提交前执行本地重复检测，预算内只重试尾帧；明确的定格镜头可在预览放行。修正已确认的 MiniMax 图像工作流 seed 绑定，保留高级覆盖、连续帧、未知提交恢复、故事与历史素材，不自动重跑实际任务。

状态：**VERIFIED（本地工程与隔离浏览器）**。完整 make check **458 passed**（131.46 秒），13 项新增回归，前后端检查与构建通过。定格保存/刷新/重跑与历史保留通过浏览器核验。实际工作流 seed 绑定已修正，202 条原记录中仅该 Workflow 改变；104 个资产可读取。正式服务已随 C34 一并加载，未自动重跑真实任务。

## 当前配置修复 C32：MiniMax 质量工作流的时长绑定

范围：按实时节点契约修正新导入的「minimax-h3-首尾帧-质量」为 17n+5、固定 24 FPS，保留配置的 6 秒上限和其他参数。复检当前短片的 6×5 秒时间线，刷新派生预算与已解决错误，保留故事、提示词、全部素材及历史作业；不自动提交真实渲染。服务空闲时加载已验证的 C31 启动修复。

状态：**VERIFIED（实际配置与数据修复）**。目标 Workflow version=3→4，时长绑定改为倍数 17、偏移 5、固定 24 FPS；13 类实时节点约束下，全部 6 镜以 5 秒/124 帧通过 patch 与依赖预检。相关回归 **16 passed**（3.09 秒）。Episode version=83→84，预算上限 4.5→6 秒、生成帧率 16→24，恢复 DRAFT/首镜 KEYFRAMES_READY；故事、提示词、素材和历史作业保持，9 张图片 HTTP 200。全库只有目标 Workflow/Episode 改变，重启后 66 条记录全部保持；C31 已加载，未提交新真实渲染。

## 当前修复 C31：删除内置工作流后不再自动恢复

范围：内置工作流仅在首次初始化配置时添加，后续启动尊重已有删除结果；兼容旧默认能力配置和首次初始化中断。验证自导入工作流删除后的完整重启路径，引用拦截明确提示“未删除”和占用短片名称。通过原删除接口移除用户指定的「Wan2.1 FLF · 首尾帧视频」，保留其他工作流、默认项、故事、作业和素材。MiniMax 需区分被旧短片引用的旧版与当前默认版本，不绕过引用保护或擅自改绑。

状态：**VERIFIED（本地工程与实际重启）；正式服务已随 C32 加载**。完整 `make check` **445 passed**（142.87 秒），前后端检查与构建通过。新增 8 项回归；实际删除 default_video 返回 204，仅该记录移除，副本连续两次初始化未恢复。C31 收尾时用户正在渲染，未中断；C32 确认空闲后重启，已删模板没有重建。MiniMax 两份的引用情况以 C31 当时记录为准，本轮未删除或擅自改绑。

## 当前审查修复 C27–C30

范围：修复帧数工作流时长预检、短视频技术校验、检测导致预览失效和 OOM 断网恢复四项已复现缺陷。保留已有故事、素材、作业快照及未知提交保护；每项独立提交，补齐隔离回归后执行完整门禁，不自动重跑真实任务。

状态：**VERIFIED（本地工程与实际数据保持）**。完整 `make check` **437 passed**（121.18 秒），Ruff/格式、ESLint、TypeScript、构建通过。C27–C30 共新增 38 项回归，正式 8000 已加载；130 条既有记录散列完全相同，当前短片 6 镜剧本、7 张参考图与首镜首尾帧保留，9 个文件 HTTP 200。未提交真实渲染，未执行远端 CI。

C27 已完成：新增 10 项回归，相关时长、预览、固定时钟与旧数据测试 **76 passed**；Ruff/格式通过。49 帧/16 FPS 约束下，10/60/90 秒计划均按最多 3 秒拆分，10 秒夹具成功导出。完整门禁随四项修复收尾执行。

C28 已完成：7 项新增视频技术校验回归及 41 项原媒体/流水线/重连/重跑测试通过。过短视频不会通过或推进后续镜头；旧错误结果继续生成时保留旧文件并正确更新连续依赖。

C29 已完成：9 项新增版本回归与 65 项预览/换绑/时长相关测试通过。反复检测和无变化保存保持预览有效，真实配置更改仍拦截；旧记录兼容，实时帧数限制仍在渲染前复检。

C30 已完成：12 项恢复回归通过，覆盖降分辨率、分段、中间帧、重启、再次断网、旧记录及本地后处理失败；已受理步骤复用原作业与配置快照，原提交次数不增加，重试预算不重置。未知提交和跨短片保护保留。当前实际短片的 OpenVDN 模型目录缺失属于远端环境问题，本轮未修改远端模型或清除其失败记录。

## 当前收尾 C26：清理过期参考图警告

范围：按当前实际 reference_image 绑定清理失效的参考图警告；更换工作流与继续生成时同步，旧记录读取只更新显示，不重跑或改写素材。保留旧工作流历史与其他警告；文生图确无输入时继续显示提示。完成后加载 C24–C26 修复并更新当前失败短片的预算，保留故事和已有首尾帧。

状态：**VERIFIED（本地工程、浏览器与实际数据修复）**。完整 `make check` **399 passed**（155.36 秒），新增 4 项回归覆盖换绑、旧数据显示兼容、继续生成真实素材绑定、缺少绑定及其他警告保持。正式 8000 已加载 C24–C26；实际短片 version=51→52、384×672，6 镜故事、7 张参考图及首镜首尾帧保持，9 个文件 HTTP 200。浏览器确认“继续生成”可用、过期警告清除，未提交真实渲染。

## 当前修复 C25：自动分辨率遵守工作流约束

范围：自动宽高在系统范围、显存预算与所选工作流 type/min/max/step/enum 的交集中向下取合法值；用户明确尺寸严格校验，OOM 降级仍可降低尺寸。规划、预览/换绑和最终渲染使用同一适配逻辑，旧预算恢复不重写故事或清空资产。同步处理实际 384×688 与 MiniMax 32 步长冲突，不自动重跑实际视频。

状态：**VERIFIED（本地工程与真实配置预检）**。完整 `make check` **395 passed**（124.59 秒），新增 18 项回归。真实 MiniMax 6 镜均通过 13 类节点预检，384×688 调整为 384×672，5 秒/124 帧/24 FPS 保持，零渲染提交。实际预算修复与服务更新已随 C26 完成。

## 当前修复 C24：取消后及时解除删除占用

范围：取消导演、分镜或 QA 模型等待及只读工作流预检时及时结束本地请求，等待清理后释放后台占用；取消尚未开始的排队短片时移出队列。保留 ComfyUI 已提交作业的原取消/未知状态保护，删除失败说明具体阻碍，不清空用户故事或资产。

状态：**VERIFIED（本地工程）；正式服务已随 C26 加载**。完整 `make check` **377 passed**（87.98 秒），新增 9 项回归；最终队列计数调整后重跑 9 项及 Ruff/格式通过。实际旧短片的占用已随队列推进解除，使用必定回滚的非法时间线请求验证同一组删除前置检查已通过，version=127 全字段不变。当时另一部短片正在真实生成，未重启服务；现已在空闲时加载，未删除用户数据。

## 当前收尾 C23：整片重新生成与旧成片预览

范围：增加直接的「整片重新生成」入口，默认重做全部启用镜头的视频，可选择重做关键帧；复用已有剧本、提示词和参考图。沿用 C21 重跑接口与历史归档，在页面直接展示旧成片版本、预览和下载，新任务运行、失败或再次重跑后仍可访问旧成片。

状态：**VERIFIED（本地工程与隔离浏览器）**。完整 `make check` **368 passed**（89.77 秒）。新增回归验证多次整片重跑、连续合成失败后所有旧成片/镜头可下载且内容逐字节不变。隔离浏览器验证取消不改数据、两种整片重跑、刷新后旧成片切换与下载；8000 已提供新页面，未提交用户真实渲染。

## 当前修复 C22：帧数工作流与 MiniMax 固定时钟

范围：视频时长可以绑定实际 length/num_frames 字段，扩展帧数偏移以支持 17n+5，增加可选固定生成 FPS；规划、预览、试跑和最终 patch 使用相同生成时钟，旧绑定保持原行为。依据实时 ComfyUI 元数据修复「首尾帧-MiniMax-真」绑定，不生成新剧本、不清空已有产物、不提交真实渲染。

状态：**VERIFIED（本地工程、浏览器与真实配置预检）**。完整 `make check` **367 passed**（87.12 秒），6 项新增回归。实际节点的 13 类依赖预检通过，5 秒换算为 124 帧/24 FPS。已通过页面把「首尾帧-MiniMax-真」保存为 version=3，绑定完整；全库散列确认只有该 Workflow 改变，全部短片/产物/默认项保持，无真实渲染提交。

## 当前迭代 C21：连接恢复与保留故事的重跑

范围：已提交 ComfyUI 作业在渲染等待期限内自动重连查询原 prompt_id，恢复 WebSocket 或轮询进度；网页断网保留最后进度，超时提供明确恢复入口，未知提交不得自动重发。增加当前镜头/全部启用镜头的仅视频或关键帧及视频重跑，保留故事、参考资产和旧版本产物，保护连续性依赖、版本冲突及未结算作业。

状态：**VERIFIED（本地工程与隔离浏览器）**。最终完整 `make check` 为 **361 passed**（86.17 秒），新增 19 项回归。浏览器验证单镜仅视频重跑、故事/关键帧/独立镜头保持、旧版本预览与下载、页面断线保留数据并自动恢复。未重新提交用户实际任务；真实模型画质和远端 CI 不包含在本轮验收。

## 当前修复 C20：配置的单镜时长优先于显存猜测

范围：移除自动低显存 3 秒和快速质量 4 秒的隐藏时长上限。统一按 Workflow 最大时长、模型参数范围/枚举/帧数约束及用户明确的单镜上限规划和校验；显存策略继续控制分辨率/batch，实际 OOM 时降分辨率、分段或使用替代工作流，保留故事时间线。旧预算在预览和续跑时刷新时长字段，不清空或重排计划/资产，不自动提交真实生成。

状态：**VERIFIED（本地工程与真实配置预检）**。完整 `make check` **342 passed**（72.06 秒），新增 11 项回归，原 OOM 用例在低显存模式通过。实际短片 18 镜均以 5 秒通过实时元数据/Resolver/最终 patch 校验，旧预算已从 3 秒修正为 5 秒，version=61，原故事和图片保持；正式 8000 已更新，未提交真实渲染。

## 历史修复 C19：失败后更换工作流保留成果与历史恢复

范围：更换工作流始终保留故事和已有素材绑定；已开始生成的短片保持确认记录，新工作流仅用于缺失结果或用户明确重试的镜头。保存不重新规划、不清空参考图/关键帧/视频、不自动生成。继续生成前按当前节点与显存重新检查预算。为旧归档重置导致的空短片增加受版本保护的恢复入口，保留当前工作流，仅恢复所选历史故事与素材，验证资产归属和文件存在；不得覆盖现有故事或未结算作业。

状态：**VERIFIED（本地工程、浏览器和实际数据恢复）**。最终完整 `make check` **331 passed**（117.94 秒），新增 16 项回归。实际短片通过恢复入口从 version=55 恢复为 56，18 镜/90 秒和 12 张图片均保持，当前三类绑定未改，没有真实生成。已更新正式服务。独立的当前新工作流 3 秒策略与原 5 秒分镜兼容性仍待处理，未通过重写故事掩盖。

## 历史修复 C18：渲染前更换工作流保留故事

范围：未产生当前绑定的渲染作业或生成素材时，更换工作流保留计划、镜头 ID/顺序/时长、视觉设定、画面提示词和用户编辑。仅更新绑定、派生预览、适用参数与预算，重新等待确认；不调用模型或自动提交渲染。不兼容的新工作流原子拒绝，不清空故事。直接修改 workflow 配置后更新预览也复用已准备内容。已有渲染结果的更换继续沿用明确的归档重置流程。

状态：**VERIFIED（本地工程、隔离浏览器与真实配置只读检查）**。完整 `make check` **315 passed**（94.17 秒），新增 11 项回归。隔离浏览器完成编辑分镜→更换图生图→保存/刷新→确认生成 10 秒 MP4，原计划/镜头 ID/用户提示词保持，LLM 调用仍为 4 次。正式 8000 已更新；用户截图所选图生图与现有 18 镜/90 秒通过只读兼容检查，实际 Episode 保持 version=29，未更换其已保存绑定或提交真实渲染。

## 历史修复 C17：云端视频预览保存校验

范围：保存/确认使用规划时同版本工作流的执行方式；旧预览可从已验证的 ComfyUI 元数据恢复视频时长节点是否为 API 节点，不按模型名称或整体 cloud 标签猜测。继续遵守 workflow 时长范围、本地图像显存策略和实时生成校验。完成 C16 的实际分镜保存。

状态：**VERIFIED（本地工程与实际数据修复）**。新增 11 项回归，完整 `make check` **304 passed**（63.75 秒）；已验证新旧云端预览保存、5 秒确认生成、90 秒计划保持及本地/未知节点限制。正式 8000 更新后，实际短片成功保存为 version=29，18×5=90 秒，第 4、7、9 镜及其他镜头使用对应画面描述；原始重复 AI 字段归档保留。全库记录散列核对仅该 Episode 改变，无实际渲染提交。

## 历史修复 C16：画面提示词单一来源与旧分镜修复

范围：首帧、尾帧、视频和参考图使用各自阶段的已准备画面提示词，禁止 Agent 在动态映射中重复填写 `prompt` 角色；保留其他 AI 参数的严格校验和用户高级覆盖。旁白脚本单独存放，不实现 TTS。旧预览只读兼容，保存时归档重复动态 prompt；修复当前 90 秒短片（含第 4、7、9 镜），保留计划、时长及原始输出，不提交视频生成。

状态：**VERIFIED（本地工程、浏览器与实际数据修复）**。完整 `make check` **293 passed**（63.13 秒），新增 15 项回归；浏览器验证旁白与画面独立展示、画面编辑/保存/刷新。实际保存时发现云端视频标识丢失，已由 C17 修正并完成 version=28→29 的持久化修复，计划、时长和原始画面描述保持。

## 历史修复 C15：空 AI 提示词与预览拦截原因

范围：修复有效分镜提示词被空白 AI 动态 prompt 覆盖，统一预览和实际渲染的回退规则；旧待确认预览读取时更新派生视图，不重新规划或改写原始记录。保持非空 AI 值、其他动态字段与显式用户覆盖优先级；校验提示指出具体镜头/字段或时长差额，未使用的 I2V 尾帧和锁定输入不作为可编辑必填项。

状态：**VERIFIED（本地工程与真实页面只读检查）**。实际 90 秒/18 镜记录的第 4、7、9 镜存在空动态 prompt，总时长本身正确。完整 `make check` **278 passed**（61.15 秒），新增 10 项空/纯空白回退、最终 patch、旧视图兼容、非法 AI 值拒绝、显式覆盖与未使用尾帧回归。浏览器验证具体字段定位和时长差额；正式服务更新后，实际短片按钮已恢复可用。原记录 version=28、计划/提示词及数据库散列未变，无真实渲染提交。

## 历史迭代 C14：分镜预览与确认生成

范围：网页创建改为“规划分镜与提示词 → 查看/修改 → 确认生成”。预览只调用导演/Bible/Shot 模型及只读工作流检查，不创建素材或提交 ComfyUI 渲染。逐镜展示动作、运镜、时长、首尾帧/视频提示词；可修改标题、时长和提示词，保存时校验总时长、单镜约束与版本。确认后复用已有计划与提示词，保持高级覆盖优先、capability 分支、连续性、QA、OOM 和拼接。保留旧记录及直接生成 API；不扩展镜头增删或重做架构。

状态：**VERIFIED（本地工程与隔离浏览器）**。完整 `make check` **268 passed**（58.76 秒），新增 16 项回归；预览零渲染、60/90 秒完整提示词、编辑进入最终 patch、覆盖优先级、非法输入原子拒绝、取消/恢复/重绑定历史及旧 API 通过。桌面完成预览→保存/刷新→多页面冲突→自动保存并确认→10 秒 MP4，390px 无横向溢出。正式 8000 已更新，原完成短片完整记录散列未变；未执行真实模型的新预览或渲染。详见 C14 验收。

## 历史迭代 C13：导演时长约束与均衡分镜

范围：将每镜最短/最长时长、建议时长/镜头数和允许镜头数写入 Agent 提示词与受约束输出 schema。默认争取每镜至少 2.5 秒，短总片长或低工作流上限时按可行解下降，用户固定时长覆盖优先。修复顺序扣减余量导致后镜越来越短的问题，保持合法原始分配及相对权重，精确满足总时长。保留现有显存、工作流、分批规划、连续性及旧计划/资产。

状态：**VERIFIED（本地工程与夹具集成）**。完整 `make check` **252 passed**（75.47 秒），新增 30 项回归；10 秒/3 秒上限为 4×2.5 秒、短片/分数边界、60/90/600 秒分批、模型输出拒绝/纠正、固定覆盖和旧计划恢复通过。真实 FFmpeg 验证新旧时间线均导出 10 秒；正式 8000 已更新，原已完成短片完整记录散列保持不变。未调用真实导演模型或重新提交真实渲染。

## 历史迭代 C12：工作流配置可用性

范围：直接展示必填输入/输出选择，所有选择只绑定真实字段；将模型与参数、绑定、检查与试跑分为明确页签，支持字段搜索和按节点查看参数，覆盖规则次级展示。保存与未保存状态常驻，缺项/错误能定位；继续保留可选 AI、无网络导入和单一参数事实源。依赖检查后明确标注包含云端 API 或本地节点，不以模型名猜测。

状态：**VERIFIED（本地工程与浏览器配置交互）**。C12 已提交 `71fe52a`，完整 `make check` **222 passed**，桌面与 390px 配置交互通过；正式服务已更新。详见 C12 验收记录；本次同步此前遗漏的任务状态。

## 历史迭代 C11：动态模型与视频时长适配

范围：读取已选 DynamicCombo 模型分支的类型、范围、步长与枚举；依据 ComfyUI 的 `api_node` 元数据区分云端视频时长预算，本地图片仍遵守显存策略。将时间线时长与合法渲染时长分开，提前检查不可满足的约束，兼容旧失败短片继续生成和已有图片；高级覆盖仍严格校验，帧数转换、QA、实际尾帧与 FFmpeg 裁切契约保留。

状态：**VERIFIED（2026-09-11）**。完整 `make check` **206 passed**（51.15 秒），新增 12 项回归：2.86 秒→4 秒整数渲染、DynamicCombo 模型切换、云端/本地预算、非法覆盖、复用旧图片恢复与真实 FFmpeg 10 秒导出。正式服务已更新，实际 MiniMax 工作流依赖检查通过，5 镜请求均通过不提交的校验；未触发真实视频渲染或远端 CI。

## 历史迭代 C10：模型超时在线配置

范围：新增 `llm_timeout`，默认 600 秒，支持设置页自定义 1～3600 秒；导演、Bible、Shot、视觉模型请求与模型测试共用此配置。保存后生效并持久化，旧配置文件缺少该字段时使用默认值。模型测试等待文案从设置读取；保留取消能力，不调整 ComfyUI 或工作流 AI 识别自身的限时策略。

状态：**VERIFIED**。完整 `make check` 为 **194 passed**（49.13 秒），新增 7 项回归并加强既有持久化与截止断言。浏览器验证 600 默认、900 保存/刷新、动态文案与上下限；正式服务重启后确认 600 秒生效。未重复调用真实模型或执行远端 CI。

## 历史迭代 C09：模型连接测试

范围：设置页分别测试已保存的导演/视觉模型，复用生成请求协议，显示结果、耗时和明确错误；视觉测试使用内置小图，不创建短片或渲染任务。测试总等待 120 秒，可取消，配置变动或离开页面丢弃旧结果。模型网络错误按安全分类返回，不回显密钥/上游原始响应。

状态：**VERIFIED（本地工程、浏览器与真实导演小请求）**。完整 `make check` 为 **187 passed**，22 项新增回归；导演/视觉夹具成功、错误提示、取消和旧结果失效通过浏览器检查。正式配置 `glm-5.3` 最小 JSON 测试 **7.82 秒通过**，真实视觉模型未配置，原失败短片未重跑。详见 [C09 验收](ACCEPTANCE.md#c09-模型测试与错误诊断2026-09-10)。

## 历史迭代 C08：短片工作流更换

范围：详情页显示并更换关键帧、文生图参考和视频工作流，可填入当前默认项。保存只进行本地校验；停止状态下按版本更新，运行中/未核对作业阻止更换。更换后归档旧生成状态，重置计划与生成结果，保留历史作业和文件；用户再点击继续生成，按新能力重新规划。保留未更换工作流的高级覆盖，移除已换走工作流的覆盖，禁止旧缓存跨绑定版本复用。

状态：**VERIFIED（本地工程与浏览器门禁）**。完整 `make check` 为 **165 passed**，新增 16 项回归；验证身份/绑定/覆盖校验、版本与活动作业保护、历史保留、新绑定缓存隔离与 2 秒上限重新分镜。浏览器完成默认填充、保存、刷新、按新绑定重新生成 4 秒夹具短片；正式服务已更新并核对原失败短片入口，未修改其绑定或触发真实生成。详见 [C08 验收](ACCEPTANCE.md#c08-短片工作流更换2026-09-10)。

## 历史修复 C07：默认工作流设置

范围：修复完整绑定但未检查依赖时“设为默认”被禁用的问题；未保存配置可一键保存并设为默认。默认选择只校验本地绑定，依赖检查和试跑保持独立，已有 Episode 的工作流 ID 不变。

状态：**VERIFIED（本地工程与浏览器门禁）**。完整 `make check` 为 **149 passed**，新增 7 项 API 回归；浏览器验证一键保存、无效编辑拦截、缺失绑定提示与刷新后默认项保留。正式页面已将用户导入的图像/视频工作流设为默认，未执行真实渲染。详见 [C07 验收](ACCEPTANCE.md#c07-默认工作流设置修复2026-09-10)。

## 历史迭代 C06：快速导入与可选 AI 识别

范围：导入只执行本地结构校验与保存，不请求 AI 或 ComfyUI；移除节点/连线规则推断，保留既有显式标签与手动配置。用户可单独触发 AI 识别并确认建议，或直接手动绑定；AI 等待可取消，失败不回退规则、不阻塞导入。依赖检查保持独立入口，生成安全校验不变。

状态：**VERIFIED（本地工程与浏览器门禁）**。完整 `make check` 为 **142 passed**，Ruff/格式、ESLint、TypeScript 与构建通过。浏览器验证直接导入、AI 建议确认、取消/识别中导入、手动优先级和仅实际字段呈现；本地导入样本 20.52ms。已重启正式本地服务；导演模型未配置，真实 AI 识别质量未验收。详见 [C06 验收](ACCEPTANCE.md#c06-快速导入ai-建议与实际字段2026-09-10)。

补充范围：移除固定角色表与动态参数表的重复呈现，参数页只列 JSON 中真实存在的可写字段，在字段上指定用途。用途选项受 capability/类型约束，文生图不展示预设素材/首尾帧项目；保留内部 owner 与绑定契约。

## 历史迭代 C05：工作流接入向导

范围：常见节点及连线自动识别输入/输出，保留已有绑定；默认展示中文概览和少量待确认项，完整绑定与帧数转换移入高级设置；按 capability 精简角色；分开显示绑定、依赖和试跑状态。不安装模型或扩展生成架构。

状态：**VERIFIED（本地工程与浏览器门禁）**。完整 `make check` 为 **127 passed**，新增 8 项识别/API 回归；Ruff/格式、ESLint、TypeScript 与构建通过。隔离浏览器完成无标签导入、歧义输出确认、保存后重载、试跑、I2V 仅首帧、自动补齐与 390px 无溢出检查。正式本地服务已加载新界面，核对缺失模型提示与绑定状态分别显示。详见 [C05 验收](ACCEPTANCE.md#c05-工作流接入向导2026-09-10)；未执行真实模型生成或远端 CI。

## 历史收尾 C04：能力分支、AI 参数与路由基础

范围固定为：I2V 只生成首帧（包括 QA、重试与 OOM 分段）；Bible/Shot 输出受当前 workflow owner/type/min/max/enum 约束的 `ai_parameters`，进入最终 patch 且用户高级覆盖优先；新增按 `default_capabilities` 查询的基础路由，兼容显式 workflow ID。保持现有架构、UI、数据库记录与生成恢复逻辑。

状态：**VERIFIED（本地工程门禁）**。完整 `make check` 为 **119 passed**，无跳过；Ruff/格式、ESLint、TypeScript 与构建全部通过。新增 28 项用例，并加强既有 I2V 断言，覆盖无尾帧/中间目标帧渲染、FLF 首尾帧、通用 AI 参数最终 patch 与非法值拒绝、四类路由及显式 ID 优先。详细证据见 [C04 验收](ACCEPTANCE.md#c04-能力分支ai-参数与路由收尾2026-09-10)。未扩展其他功能，真实模型与远端 CI 仍未执行。

## 历史迭代 C03：Workflow Capability 与参数归属

按 [C03 契约](WORKFLOW_CAPABILITIES.md) 实现四类 capability、owner/覆盖规则、真实素材自动绑定、默认/高级模式、600 秒与 240 镜统一 limits、数据库迁移及旧 API 兼容。状态：**VERIFIED**。代码、API、前端、迁移与文档已完成；`make check` 为 91 passed，默认/高级模式浏览器验收通过。

新增回归覆盖 capability 身份校验、图生图参考引导、60/90 秒规划、240 镜边界、参数优先级/锁定、用户首帧素材、旧数据迁移幂等与失败回滚。现有 QA、Continuity、OOM、FFmpeg 和在线设置回归继续通过；最终命令与证据见验收记录。

## 历史迭代 C02：成片时长（2026-09-10）

用户补充目标：预设 5/10/15/30/60/90 秒，自定义 1～600 秒；总时长与本机显卡、单镜工作流能力解耦。此前 MVP 的 5/10/15 秒仍是历史验收样本，不再限定产品输入范围。

验收项：前后端共享时长策略；600 秒计划与最多 600 镜时间线可保存；长计划分批且保留连续性/取消；真实 FFmpeg 长视频不累计时长误差；浏览器验证预设、自定义边界与移动布局。真实十分钟 AI 生成不包含在本地夹具验收中。

状态：**VERIFIED**。`make check` 为 77 passed；600 秒分批规划、600 镜时间线、90 秒模拟依赖流水线、600/598.8 秒真实 FFmpeg 合成通过。浏览器验证六个预设、1/600 秒边界、0/601/空值拦截，以及 1 秒夹具生成；390px 无横向溢出。证据见 [C02 验收](ACCEPTANCE.md#c02-总时长验收2026-09-10)。

## 冻结目标（2026-09-10）

依据原文 §2 P0、§57 Phase 1/2、§58：用户输入 Idea 与 5/10/15 秒目标时长，完成自动计划、Visual Bible、参考资产、首尾帧、ComfyUI 视频渲染、质检/局部重试、FFmpeg 合成与 MP4 下载。用户可导入 API JSON、配置绑定/参数、验证、试跑并设置默认工作流。

技术范围：本地优先，单用户、单 API 进程、单 ComfyUI、渲染并发 1；Python/FastAPI/SQLite，React/TypeScript/Vite，FFmpeg；LLM/VLM 走独立的 OpenAI-compatible JSON/vision 接口。真实模型与权重由用户环境提供。

## 任务与依赖

用户补充（2026-09-10）：先完成整体实现，不在本机部署 ComfyUI；官方 API/节点协议作为适配依据。实现和本地验证完成后，再统一请求真实验收环境。T02 是交付后的真实集成门禁，不阻塞当前编码。

当前工程交付完成；VERIFIED 表示已通过对应本地门禁，不包含 T02。完整命令、样本与限制见 [验收记录](ACCEPTANCE.md)。

| ID | 任务 | 原文依据 | 依赖 | 状态 | 验收条件 |
| --- | --- | --- | --- | --- | --- |
| D01 | 文档基线、目标、契约与验收表 | §57–65 | 无 | VERIFIED | 文档互链且明确实现/验证边界 |
| B01 | 项目骨架、配置、持久化、错误 | §6,31–38,54 | D01 | VERIFIED | lifespan 启动、SQLite 持久化、错误与 API 输入边界测试通过 |
| W01 | API JSON 分析、动态 schema、绑定与 patch | §8–11,28–30 | B01 | VERIFIED | 本地分析/patch/边界测试通过；真实节点校验待 T02 |
| W02 | ComfyUI 客户端、校验、执行、进度、取消 | §7,28,52–54 | W01 | VERIFIED | 协议夹具验证 prompt 关联、未知提交不重发、已知作业恢复与隔离取消；真实服务待 T02 |
| W03 | 默认图像/视频模板、导入与工作流库 | §12,44,58.1 | W02 | VERIFIED | 两类默认 profile、导入/参数/试跑和删除保护通过 API 与浏览器检查 |
| A01 | Director/Bible/Shot Provider 与时长规划 | §14–18,41–42 | B01 | VERIFIED | 本地 schema 修复、时长约束、QA 分流、显存预算测试通过；真实模型待 T02 |
| G01 | Episode、持久任务队列、参考与首尾帧 | §23–27,32–38,46–47 | W03,A01 | VERIFIED | 依赖模拟 + 真 FFmpeg 验证计划→资产→镜头→5 秒 MP4；API 返回异步状态 |
| G02 | 视频、连续性、VLM QA、定向重试与降级 | §19–22,39–40,53 | G01 | VERIFIED | 实际尾帧、QA 历史、视频重试、OOM 共享预算/替代模板、取消/重启回归通过 |
| M01 | 媒体校验、预览、归一化、拼接导出 | §23,49–51 | G01 | VERIFIED | 真 FFmpeg 验证混合尺寸/FPS、有声/无声拼接、H.264/音轨/时长与尾帧；API 预览和下载通过集成测试 |
| U01 | 配置引导、创建页、镜头时间线与工作流 UI | §4,48–49 | W03,G02,M01 | VERIFIED | 类型/构建/ESLint 通过；浏览器覆盖配置、表单、生成、预览、视频重试及下载链接，390px 无溢出 |
| T01 | 自动测试、模拟依赖集成、浏览器检查 | §65–66 | 上述 | VERIFIED | make check 通过，42 项测试无跳过；5/10/15 秒模拟依赖 + 真实 FFmpeg；浏览器证据已记录 |
| T02 | 真实 ComfyUI + LLM/VLM 生成与视觉验收 | §58–59 | T01,外部服务 | EXTERNAL_GATE | 5/10/15 秒 MP4；狮子案例逐项评审；记录环境 |

## 完成定义

每个实现任务包含代码、适当测试、模块/接口/运行文档更新及独立 Git commit。T01 完成只能证明本地工程闭环；项目的真实生成验收必须通过 T02，不用模拟媒体代替。性能指标 §59 需要多次实跑统计，单个成功样本不能证明成功率。

## 后续路线图

迭代 C01（2026-09-10，VERIFIED）：将连接/模型/运行参数迁移为页面配置。21 项新增回归覆盖保存与重启恢复、密钥不回显/清除、端点变更与运行中保护、无需重启生效；浏览器保存/刷新/清除/检测通过，完整 make check 为 63 passed。

按原文 Phase 3 留待后续：TTS/BGM/SFX/字幕、多 Worker、多 GPU、云 ComfyUI、Workflow 市场、自动依赖安装、批量 Idea、其他原生 Provider 适配。默认保留模型已有音轨。High 多候选策略在 MVP 质量模式中实现；外部视觉质量仍需实测。
